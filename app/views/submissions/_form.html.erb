<%= form_for(@submission) do |f| %>
	<% if @submission.errors.any? %>
		<div id="error_explanantion">
			<div pluralize(@submission.errors.count, "error") %> prevented this error from being saved:
			</div>

			<ul>
				<% @submissions.errors.ful_message.each do |message| %>
					<li>
						<%= message %>
					</li>
				<% end %>
			</ul>
		</div>
	<% end %>

	<div class="field">
		<%= f.label :company_name %>
		<%= f.text_field :company_name %>
	</div>

	<div class="field">
		<%= f.label :resume %>
		<%= f.text_field :resume %>
	</div>

	<div class="field">
		<%= f.label :cover_letter %>
		<%= f.text_field :cover_letter %>
	</div>

	<div class="field">
		<%= f.label :notes %>
		<%= f.text_field :notes %>
	</div>
<% end %>